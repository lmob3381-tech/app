<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_manage"
        android:label="GitHub Manager"
        android:theme="@style/Theme.GitHubManager"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".RepoDetailActivity" android:exported="false" />
        <activity android:name=".FileEditorActivity" android:exported="false" />
        <activity android:name=".ActionsActivity" android:exported="false" />
        <activity android:name=".WorkflowRunsActivity" android:exported="false" />
        <activity android:name=".CreateRepoActivity" android:exported="false" />

    </application>
</manifest>
