<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_light">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/brand_green"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="14dp"
        android:paddingBottom="14dp">

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Menghubungkan..."
            android:textColor="@color/white"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Quick action bar -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:scrollbars="none"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="8dp"
            android:paddingEnd="8dp">

            <Button android:id="@+id/btnHome" style="@style/QuickBtn" android:text="Beranda" />
            <Button android:id="@+id/btnUsers" style="@style/QuickBtn" android:text="Cek User" />
            <Button android:id="@+id/btnSsid1" style="@style/QuickBtn" android:text="SSID 1" />
            <Button android:id="@+id/btnSsid2" style="@style/QuickBtn" android:text="SSID 2" />
            <Button android:id="@+id/btnSsid3" style="@style/QuickBtn" android:text="SSID 3" />
            <Button android:id="@+id/btnSsid4" style="@style/QuickBtn" android:text="SSID 4" />
            <Button android:id="@+id/btnPriority" style="@style/QuickBtn" android:text="Prioritas User" />
            <Button android:id="@+id/btnWifiSettings" style="@style/QuickBtn" android:text="Pengaturan WiFi" />
            <Button android:id="@+id/btnRelogin" style="@style/QuickBtn" android:text="Login Ulang" />
        </LinearLayout>
    </HorizontalScrollView>

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:max="100"
        android:progressTint="@color/brand_green" />

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <WebView
            android:id="@+id/webView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</LinearLayout>
